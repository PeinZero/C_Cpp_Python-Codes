#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

void main() {
	int num,temp;
	printf("Enter a num\n");
	scanf("%d", & num);
	while (num/10 != 0){
		temp = num%10 ;
		printf("%d",a);
		num=num/10;
		if (num/10 == 0){
			printf("%d",num);
		}
	}
}
